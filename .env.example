
# Opcional (por defecto: gemini-flash-latest)
# Tip: si copias un nombre de la lista como `models/gemini-2.0-flash`,
# también funciona (el backend normaliza el prefijo `models/`).
GEMINI_MODEL=gemini-flash-latest

# Opcional: orígenes permitidos para CORS (separados por coma)
# Ej: http://localhost:5173,http://127.0.0.1:5173,https://pdf-chat-ui.netlify.app
CORS_ORIGINS=http://localhost:5173,https://pdf-chat-ui.netlify.app

# Opcional: regex para permitir múltiples orígenes (ej deploy previews de Netlify)
# Ej: ^https://.*--pdf-chat-ui\.netlify\.app$
# CORS_ORIGIN_REGEX=

# Opcional: limita cuánto texto del PDF se manda al modelo
MAX_CONTEXT_CHARS=60000

